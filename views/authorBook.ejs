<%- include('partials/header', { title: 'Login', isLogin }) %>
    <h2>List of Books</h2>

    <table border="1" class="table table-hover">
        <thead class="table-dark">
            <tr>
                <td>Cover</td>
                <td>Judul</td>
                <td>Tahun Terbit</td>
                <td>Publisher</td>
                <td>Stock</td>
                <td>Action</td>
            </tr>
        </thead>
        <tbody class="table-primary">
            <% books.forEach(book => { %>
                <tr>
                    <td><img src="<%= book.cover %>" height="200"></td>
                    <td><%= book.judul %></td>
                    <td><%= book.tahun_terbit %></td>
                    <td>
                        <% if (book.Publisher) { %>
                            <%= book.Publisher.name %>
                        <% } else { %>
                            Unknown
                        <% } %>
                    </td>
                    <td><%= book.stock %></td>
                    <td>
                        <a href="/books/<%= book.id %>/rent">Rent</a> |
                        <a href="/books/<%= book.id %>/edit">Edit</a>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
<%- include('partials/footer') %>
